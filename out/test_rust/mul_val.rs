//! CAN DBC encoder/decoder generated by dbcc. Do not edit.
//! The module is dependency-free and can be used in `no_std` crates.
#![allow(non_camel_case_types, non_snake_case, non_upper_case_globals)]

pub const DBCC_GENERATOR_VERSION: u32 = 3u32;
pub const DBCC_HASH: u32 = 0xda51f930u32;
pub const DBCC_MODULE_NAME: &str = "mul_val";
pub const DBCC_NODE_HASH_VECTOR__XXX: u32 = 0xc719dc00u32;

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum DbccError {
    UnknownMessage { id: u32 },
    InvalidDlc { id: u32, expected: u8, actual: u8 },
    InactiveMultiplexedSignal { signal: &'static str, mux: u64 },
    OutOfRange { signal: &'static str },
    InvalidEnumValue { signal: &'static str, value: u64 },
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct CanFrame { pub id: u32, pub dlc: u8, pub payload: u64 }

#[derive(Debug, Clone, Copy, PartialEq)]
pub struct SignalInfo {
    pub name: &'static str,
    pub start_bit: u16,
    pub bit_length: u16,
    pub little_endian: bool,
    pub signed: bool,
    pub floating: bool,
    pub scaling: f64,
    pub offset: f64,
    pub minimum: f64,
    pub maximum: f64,
    pub units: &'static str,
    pub multiplexor: bool,
    pub multiplexed: bool,
    pub multiplexor_name: Option<&'static str>,
    pub switch_value: Option<u64>,
    pub switch_ranges: &'static [(u64, u64)],
}

#[derive(Debug, Clone, Copy, PartialEq)]
pub struct MessageInfo {
    pub id: u32,
    pub dlc: u8,
    pub name: &'static str,
    pub is_extended: bool,
    pub cycle_time_ms: Option<u32>,
    pub signals: &'static [SignalInfo],
}

#[inline]
const fn dbcc_mask(bits: u32) -> u64 {
    if bits >= 64 { u64::MAX } else { (1u64 << bits) - 1u64 }
}

#[inline]
fn dbcc_extract(payload: u64, start: u32, bits: u32, motorola: bool) -> u64 {
    let lane = if motorola { payload.swap_bytes() } else { payload };
    (lane >> start) & dbcc_mask(bits)
}

#[inline]
fn dbcc_insert(payload: &mut u64, start: u32, bits: u32, motorola: bool, value: u64) {
    let mask = dbcc_mask(bits);
    let mut lane = if motorola { payload.swap_bytes() } else { *payload };
    lane = (lane & !(mask << start)) | ((value & mask) << start);
    *payload = if motorola { lane.swap_bytes() } else { lane };
}

pub const CAN_ID_EXTENDED_MULTIPLEX_W_RANGES: u32 = 0u32;
pub const CAN_DLC_EXTENDED_MULTIPLEX_W_RANGES: u8 = 2u8;
pub const EXTENDED_MULTIPLEX_W_RANGES_MUXER_W_MULTIPLE_RANGES_MIN: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_W_RANGES_MUXER_W_MULTIPLE_RANGES_MAX: f64 = 255_f64;
pub const EXTENDED_MULTIPLEX_W_RANGES_MUXER_W_MULTIPLE_RANGES_SCALING: f64 = 1_f64;
pub const EXTENDED_MULTIPLEX_W_RANGES_MUXER_W_MULTIPLE_RANGES_OFFSET: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_W_RANGES_MUXED_W_RANGES_MIN: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_W_RANGES_MUXED_W_RANGES_MAX: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_W_RANGES_MUXED_W_RANGES_SCALING: f64 = 1_f64;
pub const EXTENDED_MULTIPLEX_W_RANGES_MUXED_W_RANGES_OFFSET: f64 = 0_f64;

#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub struct ExtendedMultiplexWRanges { payload: u64 }

impl ExtendedMultiplexWRanges {
    pub const ID: u32 = CAN_ID_EXTENDED_MULTIPLEX_W_RANGES;
    pub const DLC: u8 = CAN_DLC_EXTENDED_MULTIPLEX_W_RANGES;
    pub const fn new() -> Self { Self { payload: 0 } }
    pub const fn from_payload(payload: u64) -> Self { Self { payload } }
    pub const fn payload(&self) -> u64 { self.payload }
    pub fn set_payload(&mut self, payload: u64) { self.payload = payload; }
    pub const fn frame(&self) -> CanFrame {
        CanFrame { id: Self::ID, dlc: Self::DLC, payload: self.payload }
    }

    pub fn get_muxer_w_multiple_ranges(&self) -> Result<u8, DbccError> {
        let raw = dbcc_extract(self.payload, 0u32, 8u32, false);
        let value = raw as u8;
        if (value as f64) < 0_f64 || (value as f64) > 255_f64 {
            return Err(DbccError::OutOfRange { signal: "muxer_w_multiple_ranges" });
        }
        Ok(value)
    }

    pub fn set_muxer_w_multiple_ranges(&mut self, value: u8) -> Result<(), DbccError> {
        if (value as f64) < 0_f64 || (value as f64) > 255_f64 {
            return Err(DbccError::OutOfRange { signal: "muxer_w_multiple_ranges" });
        }
        let raw = value as u64;
        dbcc_insert(&mut self.payload, 0u32, 8u32, false, raw);
        Ok(())
    }

    pub fn get_muxed_w_ranges(&self) -> Result<i8, DbccError> {
        let mux = dbcc_extract(self.payload, 0u32, 8u32, false);
        if !((mux >= 1u64 && mux <= 2u64) || (mux >= 66u64 && mux <= 66u64) || (mux >= 255u64 && mux <= 255u64)) {
            return Err(DbccError::InactiveMultiplexedSignal { signal: "muxed_w_ranges", mux });
        }
        let raw = dbcc_extract(self.payload, 8u32, 8u32, false);
        let signed = if raw & (1u64 << 7u32) != 0 {
            (raw | !dbcc_mask(8u32)) as i64
        } else { raw as i64 };
        let value = signed as i8;
        Ok(value)
    }

    pub fn set_muxed_w_ranges(&mut self, value: i8) -> Result<(), DbccError> {
        let mux = dbcc_extract(self.payload, 0u32, 8u32, false);
        if !((mux >= 1u64 && mux <= 2u64) || (mux >= 66u64 && mux <= 66u64) || (mux >= 255u64 && mux <= 255u64)) {
            return Err(DbccError::InactiveMultiplexedSignal { signal: "muxed_w_ranges", mux });
        }
        let raw = (value as i64) as u64;
        dbcc_insert(&mut self.payload, 8u32, 8u32, false, raw);
        Ok(())
    }

}

pub fn decode_0x000_muxer_w_multiple_ranges(message: &ExtendedMultiplexWRanges) -> Result<u8, DbccError> {
    message.get_muxer_w_multiple_ranges()
}

pub fn encode_0x000_muxer_w_multiple_ranges(message: &mut ExtendedMultiplexWRanges, value: u8) -> Result<(), DbccError> {
    message.set_muxer_w_multiple_ranges(value)
}

pub fn decode_0x000_muxed_w_ranges(message: &ExtendedMultiplexWRanges) -> Result<i8, DbccError> {
    message.get_muxed_w_ranges()
}

pub fn encode_0x000_muxed_w_ranges(message: &mut ExtendedMultiplexWRanges, value: i8) -> Result<(), DbccError> {
    message.set_muxed_w_ranges(value)
}

pub const CAN_ID_EXTENDED_MULTIPLEX: u32 = 1682u32;
pub const CAN_DLC_EXTENDED_MULTIPLEX: u8 = 8u8;
pub const EXTENDED_MULTIPLEX_SIG1_MIN: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_SIG1_MAX: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_SIG1_SCALING: f64 = 1_f64;
pub const EXTENDED_MULTIPLEX_SIG1_OFFSET: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_SIG2_MIN: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_SIG2_MAX: f64 = 4_f64;
pub const EXTENDED_MULTIPLEX_SIG2_SCALING: f64 = -1_f64;
pub const EXTENDED_MULTIPLEX_SIG2_OFFSET: f64 = 4_f64;
pub const EXTENDED_MULTIPLEX_SIG3_MIN: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_SIG3_MAX: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_SIG3_SCALING: f64 = 1_f64;
pub const EXTENDED_MULTIPLEX_SIG3_OFFSET: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_SIMPLE_MUXER_MIN: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_SIMPLE_MUXER_MAX: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_SIMPLE_MUXER_SCALING: f64 = 1_f64;
pub const EXTENDED_MULTIPLEX_SIMPLE_MUXER_OFFSET: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_MUXED_MUXER_MIN: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_MUXED_MUXER_MAX: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_MUXED_MUXER_SCALING: f64 = 1_f64;
pub const EXTENDED_MULTIPLEX_MUXED_MUXER_OFFSET: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_MUXED1_MIN: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_MUXED1_MAX: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_MUXED1_SCALING: f64 = 1_f64;
pub const EXTENDED_MULTIPLEX_MUXED1_OFFSET: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_MUXED2_MIN: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_MUXED2_MAX: f64 = 0_f64;
pub const EXTENDED_MULTIPLEX_MUXED2_SCALING: f64 = 1_f64;
pub const EXTENDED_MULTIPLEX_MUXED2_OFFSET: f64 = 0_f64;

#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub struct ExtendedMultiplex { payload: u64 }

impl ExtendedMultiplex {
    pub const ID: u32 = CAN_ID_EXTENDED_MULTIPLEX;
    pub const DLC: u8 = CAN_DLC_EXTENDED_MULTIPLEX;
    pub const fn new() -> Self { Self { payload: 0 } }
    pub const fn from_payload(payload: u64) -> Self { Self { payload } }
    pub const fn payload(&self) -> u64 { self.payload }
    pub fn set_payload(&mut self, payload: u64) { self.payload = payload; }
    pub const fn frame(&self) -> CanFrame {
        CanFrame { id: Self::ID, dlc: Self::DLC, payload: self.payload }
    }

    pub fn get_sig1(&self) -> Result<u8, DbccError> {
        let raw = dbcc_extract(self.payload, 0u32, 2u32, false);
        let value = raw as u8;
        Ok(value)
    }

    pub fn set_sig1(&mut self, value: u8) -> Result<(), DbccError> {
        let raw = value as u64;
        dbcc_insert(&mut self.payload, 0u32, 2u32, false, raw);
        Ok(())
    }

    pub fn get_sig2(&self) -> Result<f32, DbccError> {
        let raw = dbcc_extract(self.payload, 2u32, 2u32, false);
        let value = raw as f32 * -1_f32 + 4_f32;
        if (value as f64) < 0_f64 || (value as f64) > 4_f64 {
            return Err(DbccError::OutOfRange { signal: "sig2" });
        }
        Ok(value)
    }

    pub fn set_sig2(&mut self, value: f32) -> Result<(), DbccError> {
        if (value as f64) < 0_f64 || (value as f64) > 4_f64 {
            return Err(DbccError::OutOfRange { signal: "sig2" });
        }
        let mut wire = value as f64;
        wire -= 4_f64;
        wire /= -1_f64;
        let raw = wire.round() as u64;
        dbcc_insert(&mut self.payload, 2u32, 2u32, false, raw);
        Ok(())
    }

    pub fn get_sig3(&self) -> Result<u8, DbccError> {
        let raw = dbcc_extract(self.payload, 4u32, 4u32, false);
        let value = raw as u8;
        Ok(value)
    }

    pub fn set_sig3(&mut self, value: u8) -> Result<(), DbccError> {
        let raw = value as u64;
        dbcc_insert(&mut self.payload, 4u32, 4u32, false, raw);
        Ok(())
    }

    pub fn get_simple_muxer(&self) -> Result<u16, DbccError> {
        let raw = dbcc_extract(self.payload, 8u32, 16u32, false);
        let value = raw as u16;
        Ok(value)
    }

    pub fn set_simple_muxer(&mut self, value: u16) -> Result<(), DbccError> {
        let raw = value as u64;
        dbcc_insert(&mut self.payload, 8u32, 16u32, false, raw);
        Ok(())
    }

    pub fn get_muxed_muxer(&self) -> Result<u8, DbccError> {
        let mux = dbcc_extract(self.payload, 8u32, 16u32, false);
        if !((mux >= 9216u64 && mux <= 9216u64)) {
            return Err(DbccError::InactiveMultiplexedSignal { signal: "muxed_muxer", mux });
        }
        let raw = dbcc_extract(self.payload, 24u32, 8u32, false);
        let value = raw as u8;
        Ok(value)
    }

    pub fn set_muxed_muxer(&mut self, value: u8) -> Result<(), DbccError> {
        let mux = dbcc_extract(self.payload, 8u32, 16u32, false);
        if !((mux >= 9216u64 && mux <= 9216u64)) {
            return Err(DbccError::InactiveMultiplexedSignal { signal: "muxed_muxer", mux });
        }
        let raw = value as u64;
        dbcc_insert(&mut self.payload, 24u32, 8u32, false, raw);
        Ok(())
    }

    pub fn get_muxed1(&self) -> Result<u32, DbccError> {
        let mux = dbcc_extract(self.payload, 8u32, 16u32, false);
        if !((mux >= 9216u64 && mux <= 9216u64)) {
            return Err(DbccError::InactiveMultiplexedSignal { signal: "muxed1", mux });
        }
        let mux = dbcc_extract(self.payload, 24u32, 8u32, false);
        if !((mux >= 1u64 && mux <= 1u64)) {
            return Err(DbccError::InactiveMultiplexedSignal { signal: "muxed1", mux });
        }
        let raw = dbcc_extract(self.payload, 32u32, 32u32, false);
        let value = raw as u32;
        Ok(value)
    }

    pub fn set_muxed1(&mut self, value: u32) -> Result<(), DbccError> {
        let mux = dbcc_extract(self.payload, 8u32, 16u32, false);
        if !((mux >= 9216u64 && mux <= 9216u64)) {
            return Err(DbccError::InactiveMultiplexedSignal { signal: "muxed1", mux });
        }
        let mux = dbcc_extract(self.payload, 24u32, 8u32, false);
        if !((mux >= 1u64 && mux <= 1u64)) {
            return Err(DbccError::InactiveMultiplexedSignal { signal: "muxed1", mux });
        }
        let raw = value as u64;
        dbcc_insert(&mut self.payload, 32u32, 32u32, false, raw);
        Ok(())
    }

    pub fn get_muxed2(&self) -> Result<u32, DbccError> {
        let mux = dbcc_extract(self.payload, 8u32, 16u32, false);
        if !((mux >= 9216u64 && mux <= 9216u64)) {
            return Err(DbccError::InactiveMultiplexedSignal { signal: "muxed2", mux });
        }
        let mux = dbcc_extract(self.payload, 24u32, 8u32, false);
        if !((mux >= 4u64 && mux <= 4u64)) {
            return Err(DbccError::InactiveMultiplexedSignal { signal: "muxed2", mux });
        }
        let raw = dbcc_extract(self.payload, 32u32, 32u32, false);
        let value = raw as u32;
        Ok(value)
    }

    pub fn set_muxed2(&mut self, value: u32) -> Result<(), DbccError> {
        let mux = dbcc_extract(self.payload, 8u32, 16u32, false);
        if !((mux >= 9216u64 && mux <= 9216u64)) {
            return Err(DbccError::InactiveMultiplexedSignal { signal: "muxed2", mux });
        }
        let mux = dbcc_extract(self.payload, 24u32, 8u32, false);
        if !((mux >= 4u64 && mux <= 4u64)) {
            return Err(DbccError::InactiveMultiplexedSignal { signal: "muxed2", mux });
        }
        let raw = value as u64;
        dbcc_insert(&mut self.payload, 32u32, 32u32, false, raw);
        Ok(())
    }

}

pub fn decode_0x692_sig1(message: &ExtendedMultiplex) -> Result<u8, DbccError> {
    message.get_sig1()
}

pub fn encode_0x692_sig1(message: &mut ExtendedMultiplex, value: u8) -> Result<(), DbccError> {
    message.set_sig1(value)
}

pub fn decode_0x692_sig2(message: &ExtendedMultiplex) -> Result<f32, DbccError> {
    message.get_sig2()
}

pub fn encode_0x692_sig2(message: &mut ExtendedMultiplex, value: f32) -> Result<(), DbccError> {
    message.set_sig2(value)
}

pub fn decode_0x692_sig3(message: &ExtendedMultiplex) -> Result<u8, DbccError> {
    message.get_sig3()
}

pub fn encode_0x692_sig3(message: &mut ExtendedMultiplex, value: u8) -> Result<(), DbccError> {
    message.set_sig3(value)
}

pub fn decode_0x692_simple_muxer(message: &ExtendedMultiplex) -> Result<u16, DbccError> {
    message.get_simple_muxer()
}

pub fn encode_0x692_simple_muxer(message: &mut ExtendedMultiplex, value: u16) -> Result<(), DbccError> {
    message.set_simple_muxer(value)
}

pub fn decode_0x692_muxed_muxer(message: &ExtendedMultiplex) -> Result<u8, DbccError> {
    message.get_muxed_muxer()
}

pub fn encode_0x692_muxed_muxer(message: &mut ExtendedMultiplex, value: u8) -> Result<(), DbccError> {
    message.set_muxed_muxer(value)
}

pub fn decode_0x692_muxed1(message: &ExtendedMultiplex) -> Result<u32, DbccError> {
    message.get_muxed1()
}

pub fn encode_0x692_muxed1(message: &mut ExtendedMultiplex, value: u32) -> Result<(), DbccError> {
    message.set_muxed1(value)
}

pub fn decode_0x692_muxed2(message: &ExtendedMultiplex) -> Result<u32, DbccError> {
    message.get_muxed2()
}

pub fn encode_0x692_muxed2(message: &mut ExtendedMultiplex, value: u32) -> Result<(), DbccError> {
    message.set_muxed2(value)
}

pub static SIGNALS_EXTENDED_MULTIPLEX_W_RANGES: &[SignalInfo] = &[
    SignalInfo { name: "muxer_w_multiple_ranges", start_bit: 0u16, bit_length: 8u16, little_endian: true, signed: false, floating: false, scaling: 1_f64, offset: 0_f64, minimum: 0_f64, maximum: 255_f64, units: "", multiplexor: true, multiplexed: false, multiplexor_name: None, switch_value: None, switch_ranges: &[] },
    SignalInfo { name: "muxed_w_ranges", start_bit: 8u16, bit_length: 8u16, little_endian: true, signed: true, floating: false, scaling: 1_f64, offset: 0_f64, minimum: 0_f64, maximum: 0_f64, units: "", multiplexor: false, multiplexed: true, multiplexor_name: Some("muxer_w_multiple_ranges"), switch_value: Some(1u64), switch_ranges: &[(1u64, 2u64), (66u64, 66u64), (255u64, 255u64)] },
];

pub static SIGNALS_EXTENDED_MULTIPLEX: &[SignalInfo] = &[
    SignalInfo { name: "sig1", start_bit: 0u16, bit_length: 2u16, little_endian: true, signed: false, floating: false, scaling: 1_f64, offset: 0_f64, minimum: 0_f64, maximum: 0_f64, units: "", multiplexor: false, multiplexed: false, multiplexor_name: None, switch_value: None, switch_ranges: &[] },
    SignalInfo { name: "sig2", start_bit: 2u16, bit_length: 2u16, little_endian: true, signed: false, floating: false, scaling: -1_f64, offset: 4_f64, minimum: 0_f64, maximum: 4_f64, units: "", multiplexor: false, multiplexed: false, multiplexor_name: None, switch_value: None, switch_ranges: &[] },
    SignalInfo { name: "sig3", start_bit: 4u16, bit_length: 4u16, little_endian: true, signed: false, floating: false, scaling: 1_f64, offset: 0_f64, minimum: 0_f64, maximum: 0_f64, units: "", multiplexor: false, multiplexed: false, multiplexor_name: None, switch_value: None, switch_ranges: &[] },
    SignalInfo { name: "simple_muxer", start_bit: 8u16, bit_length: 16u16, little_endian: true, signed: false, floating: false, scaling: 1_f64, offset: 0_f64, minimum: 0_f64, maximum: 0_f64, units: "", multiplexor: true, multiplexed: false, multiplexor_name: None, switch_value: None, switch_ranges: &[] },
    SignalInfo { name: "muxed_muxer", start_bit: 24u16, bit_length: 8u16, little_endian: true, signed: false, floating: false, scaling: 1_f64, offset: 0_f64, minimum: 0_f64, maximum: 0_f64, units: "", multiplexor: true, multiplexed: true, multiplexor_name: Some("simple_muxer"), switch_value: Some(9216u64), switch_ranges: &[(9216u64, 9216u64)] },
    SignalInfo { name: "muxed1", start_bit: 32u16, bit_length: 32u16, little_endian: true, signed: false, floating: false, scaling: 1_f64, offset: 0_f64, minimum: 0_f64, maximum: 0_f64, units: "", multiplexor: false, multiplexed: true, multiplexor_name: Some("muxed_muxer"), switch_value: Some(1u64), switch_ranges: &[(1u64, 1u64)] },
    SignalInfo { name: "muxed2", start_bit: 32u16, bit_length: 32u16, little_endian: true, signed: false, floating: false, scaling: 1_f64, offset: 0_f64, minimum: 0_f64, maximum: 0_f64, units: "", multiplexor: false, multiplexed: true, multiplexor_name: Some("muxed_muxer"), switch_value: Some(4u64), switch_ranges: &[(4u64, 4u64)] },
];

pub static ALL_MESSAGES: &[MessageInfo] = &[
    MessageInfo { id: CAN_ID_EXTENDED_MULTIPLEX_W_RANGES, dlc: CAN_DLC_EXTENDED_MULTIPLEX_W_RANGES, name: "extended_multiplex_w_ranges", is_extended: false, cycle_time_ms: None, signals: SIGNALS_EXTENDED_MULTIPLEX_W_RANGES },
    MessageInfo { id: CAN_ID_EXTENDED_MULTIPLEX, dlc: CAN_DLC_EXTENDED_MULTIPLEX, name: "extended_multiplex", is_extended: false, cycle_time_ms: None, signals: SIGNALS_EXTENDED_MULTIPLEX },
];

/// Return metadata for every CAN message in the DBC.
pub const fn get_all_mess() -> &'static [MessageInfo] { ALL_MESSAGES }

pub fn get_message(id: u32) -> Option<&'static MessageInfo> {
    ALL_MESSAGES.iter().find(|message| message.id == id)
}

pub fn get_all_signals(id: u32) -> Option<&'static [SignalInfo]> {
    get_message(id).map(|message| message.signals)
}

pub fn get_signal(id: u32, name: &str) -> Option<&'static SignalInfo> {
    get_all_signals(id)?.iter().find(|signal| signal.name == name)
}

pub fn message_dlc(id: u32) -> Option<u8> { get_message(id).map(|m| m.dlc) }

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum Message {
    ExtendedMultiplexWRanges(ExtendedMultiplexWRanges),
    ExtendedMultiplex(ExtendedMultiplex),
}

impl Message {
    pub const fn id(&self) -> u32 {
        match self {
            Self::ExtendedMultiplexWRanges(_) => ExtendedMultiplexWRanges::ID,
            Self::ExtendedMultiplex(_) => ExtendedMultiplex::ID,
        }
    }

    pub const fn dlc(&self) -> u8 {
        match self {
            Self::ExtendedMultiplexWRanges(_) => ExtendedMultiplexWRanges::DLC,
            Self::ExtendedMultiplex(_) => ExtendedMultiplex::DLC,
        }
    }

    pub const fn payload(&self) -> u64 {
        match self {
            Self::ExtendedMultiplexWRanges(message) => message.payload(),
            Self::ExtendedMultiplex(message) => message.payload(),
        }
    }

    pub const fn frame(&self) -> CanFrame {
        CanFrame { id: self.id(), dlc: self.dlc(), payload: self.payload() }
    }

    pub const fn get_extended_multiplex_w_ranges(&self) -> Option<&ExtendedMultiplexWRanges> {
        match self { Self::ExtendedMultiplexWRanges(message) => Some(message), _ => None }
    }

    pub fn get_extended_multiplex_w_ranges_mut(&mut self) -> Option<&mut ExtendedMultiplexWRanges> {
        match self { Self::ExtendedMultiplexWRanges(message) => Some(message), _ => None }
    }

    pub const fn get_extended_multiplex(&self) -> Option<&ExtendedMultiplex> {
        match self { Self::ExtendedMultiplex(message) => Some(message), _ => None }
    }

    pub fn get_extended_multiplex_mut(&mut self) -> Option<&mut ExtendedMultiplex> {
        match self { Self::ExtendedMultiplex(message) => Some(message), _ => None }
    }

}

pub fn decode_message(id: u32, payload: u64, dlc: u8) -> Result<Message, DbccError> {
    match id {
        CAN_ID_EXTENDED_MULTIPLEX_W_RANGES => {
            if dlc != CAN_DLC_EXTENDED_MULTIPLEX_W_RANGES {
                return Err(DbccError::InvalidDlc { id, expected: CAN_DLC_EXTENDED_MULTIPLEX_W_RANGES, actual: dlc });
            }
            Ok(Message::ExtendedMultiplexWRanges(ExtendedMultiplexWRanges::from_payload(payload)))
        }
        CAN_ID_EXTENDED_MULTIPLEX => {
            if dlc != CAN_DLC_EXTENDED_MULTIPLEX {
                return Err(DbccError::InvalidDlc { id, expected: CAN_DLC_EXTENDED_MULTIPLEX, actual: dlc });
            }
            Ok(Message::ExtendedMultiplex(ExtendedMultiplex::from_payload(payload)))
        }
        _ => Err(DbccError::UnknownMessage { id }),
    }
}

